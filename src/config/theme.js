import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1A6B0A',
    },
    secondary: {
      main: '#F7F4E9',
    },
  },
  typography: {
    fontFamily: 'serif',
  },
});

export default theme;
